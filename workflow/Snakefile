# HyARQ Snakemake workflow
# Main workflow entry point.

from pathlib import Path


WORKFLOW_DIR = Path(workflow.basedir).resolve()
REPOSITORY_ROOT = WORKFLOW_DIR.parent

BUILD_CONCATENATED_REFERENCE_SCRIPT = str(
    WORKFLOW_DIR / "scripts" / "build_concatenated_reference.sh"
)
BUILD_CONCATENATED_GTF_SCRIPT = str(
    WORKFLOW_DIR / "scripts" / "build_concatenated_gtf.sh"
)
VALIDATE_PAIRED_FEATURES_SCRIPT = str(
    WORKFLOW_DIR / "scripts" / "validate_paired_features.py"
)
MAP_RNA_STAR_SCRIPT = str(WORKFLOW_DIR / "scripts" / "map_rna_star.sh")
MAP_MOA_STAR_SCRIPT = str(WORKFLOW_DIR / "scripts" / "map_moa_star.sh")
MAP_ATAC_STAR_SCRIPT = str(WORKFLOW_DIR / "scripts" / "map_atac_star.sh")
MAP_CHIP_STAR_SCRIPT = str(
    WORKFLOW_DIR / "scripts" / "map_chip_star.sh"
)
DEDUPLICATE_ATAC_SCRIPT = str(
    WORKFLOW_DIR / "scripts" / "deduplicate_atac.sh"
)
DEDUPLICATE_CHIP_SCRIPT = str(
    WORKFLOW_DIR / "scripts" / "deduplicate_chip.sh"
)
COUNT_RNA_GENES_SCRIPT = str(
    WORKFLOW_DIR / "scripts" / "count_rna_genes.sh"
)
COUNT_PAIRED_REGIONS_SCRIPT = str(
    WORKFLOW_DIR / "scripts" / "count_paired_regions.py"
)
AGGREGATE_RNA_GENE_COUNTS_SCRIPT = str(
    WORKFLOW_DIR / "scripts" / "aggregate_rna_gene_counts.py"
)
AGGREGATE_PAIRED_REGION_COUNTS_SCRIPT = str(
    WORKFLOW_DIR / "scripts" / "aggregate_paired_region_counts.py"
)
RNA_GENE_QC_SCRIPT = str(WORKFLOW_DIR / "scripts" / "rna_gene_qc.py")
PEAK_PAIR_QC_SCRIPT = str(
    WORKFLOW_DIR / "scripts" / "peak_pair_qc.py"
)
MULTIQC_CONFIG = str(REPOSITORY_ROOT / "config" / "multiqc_config.yaml")


configfile: str(REPOSITORY_ROOT / "config" / "config.yaml")

include: "rules/common.smk"
include: "rules/reference.smk"
include: "rules/preprocessing.smk"
include: "rules/rna.smk"
include: "rules/atac.smk"
include: "rules/chip.smk"
include: "rules/moa.smk"
include: "rules/reports.smk"

FINAL_TARGETS = []

if RNA_LIBRARIES:
    FINAL_TARGETS.extend(
        expand(
            "results/counts/rna/{library_id}/gene_counts.tsv",
            library_id=RNA_LIBRARIES,
        )
    )
    FINAL_TARGETS.extend(
        expand(
            "results/counts/rna/{library_id}/filter_summary.tsv",
            library_id=RNA_LIBRARIES,
        )
    )
    FINAL_TARGETS.extend(
        [
            "results/counts/rna/gene_pair_count_matrix.tsv",
            "results/reports/rna/multiqc_report.html",
        ]
    )
    FINAL_TARGETS.extend(RNA_GENE_QC_OUTPUTS)

if MOA_LIBRARIES:
    FINAL_TARGETS.extend(
        expand(
            "results/counts/moa/{library_id}/peak_counts.tsv",
            library_id=MOA_LIBRARIES,
        )
    )
    FINAL_TARGETS.extend(
        expand(
            "results/counts/moa/{library_id}/filter_summary.tsv",
            library_id=MOA_LIBRARIES,
        )
    )
    FINAL_TARGETS.extend(
        [
            "results/counts/moa/peak_pair_count_matrix.tsv",
            "results/reports/moa/multiqc_report.html",
        ]
    )
    FINAL_TARGETS.extend(MOA_PEAK_QC_OUTPUTS)

if ATAC_LIBRARIES:
    FINAL_TARGETS.extend(
        expand(
            "results/qc/fastqc/raw/{library_id}",
            library_id=ATAC_LIBRARIES,
        )
    )
    FINAL_TARGETS.extend(
        expand(
            "results/qc/fastqc/clean/atac/{library_id}",
            library_id=ATAC_LIBRARIES,
        )
    )
    FINAL_TARGETS.extend(
        expand(
            "results/mapping/atac/{library_id}.unique.bam",
            library_id=ATAC_LIBRARIES,
        )
    )
    FINAL_TARGETS.extend(
        expand(
            "results/mapping/atac/{library_id}.mapping_summary.tsv",
            library_id=ATAC_LIBRARIES,
        )
    )
    FINAL_TARGETS.extend(
        expand(
            "results/mapping/atac/{library_id}.dedup.bam",
            library_id=ATAC_LIBRARIES,
        )
    )
    FINAL_TARGETS.extend(
        expand(
            "results/qc/samtools/atac/{library_id}.markdup.txt",
            library_id=ATAC_LIBRARIES,
        )
    )
    FINAL_TARGETS.extend(
        expand(
            "results/counts/atac/{library_id}/peak_counts.tsv",
            library_id=ATAC_LIBRARIES,
        )
    )
    FINAL_TARGETS.extend(
        expand(
            "results/counts/atac/{library_id}/filter_summary.tsv",
            library_id=ATAC_LIBRARIES,
        )
    )
    FINAL_TARGETS.extend(
        [
            "results/counts/atac/peak_pair_count_matrix.tsv",
            "results/reports/atac/multiqc_report.html",
        ]
    )
    FINAL_TARGETS.append(
        "results/qc/atac/peak_pairs_validation.tsv"
    )
    FINAL_TARGETS.extend(ATAC_PEAK_QC_OUTPUTS)

if CHIP_LIBRARIES:
    FINAL_TARGETS.extend(
        expand(
            "results/qc/fastqc/raw/{library_id}",
            library_id=CHIP_LIBRARIES,
        )
    )
    FINAL_TARGETS.extend(
        expand(
            "results/qc/fastqc/clean/chip/{library_id}",
            library_id=CHIP_LIBRARIES,
        )
    )
    FINAL_TARGETS.extend(
        expand(
            "results/mapping/chip/{library_id}.unique.bam",
            library_id=CHIP_LIBRARIES,
        )
    )
    FINAL_TARGETS.extend(
        expand(
            "results/mapping/chip/{library_id}.mapping_summary.tsv",
            library_id=CHIP_LIBRARIES,
        )
    )
    FINAL_TARGETS.extend(
        expand(
            "results/mapping/chip/{library_id}.dedup.bam",
            library_id=CHIP_LIBRARIES,
        )
    )
    FINAL_TARGETS.extend(
        expand(
            "results/qc/samtools/chip/{library_id}.markdup.txt",
            library_id=CHIP_LIBRARIES,
        )
    )
    FINAL_TARGETS.extend(
        expand(
            "results/counts/chip/{library_id}/peak_counts.tsv",
            library_id=CHIP_LIBRARIES,
        )
    )
    FINAL_TARGETS.extend(
        expand(
            "results/counts/chip/{library_id}/filter_summary.tsv",
            library_id=CHIP_LIBRARIES,
        )
    )
    FINAL_TARGETS.extend(
        [
            "results/counts/chip/peak_pair_count_matrix.tsv",
            "results/reports/chip/multiqc_report.html",
        ]
    )
    FINAL_TARGETS.append(
        "results/qc/chip/peak_pairs_validation.tsv"
    )
    FINAL_TARGETS.extend(CHIP_PEAK_QC_OUTPUTS)


rule all:
    input:
        FINAL_TARGETS
